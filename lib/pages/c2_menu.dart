import 'package:flutter/material.dart';
 
class c2Menu extends StatelessWidget {
  const c2Menu({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('MENU : C2'),

      )
    );

  }
}