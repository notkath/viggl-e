import 'package:flutter/material.dart';
 
class c3Menu extends StatelessWidget {
  const c3Menu({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('MENU : C3'),

      )
    );

  }
}