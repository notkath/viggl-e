import 'package:flutter/material.dart';
 
class c4Menu extends StatelessWidget {
  const c4Menu({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('MENU : C4'),

      )
    );

  }
}